package stepDefs;

import org.junit.Assert;

import fixtureUtils.ExcelActions;
import fixtureUtils.FixtureManager;
import io.cucumber.java.en.Given;
import webActions.ActionStoreData;

public class StepDefStoreData {
	
	/*
	 * @Given("user captures {string} into {string}") public void
	 * user_captures_into(String LocatorOfData, String colName) {
	 * 
	 * 
	 * Assert.assertTrue(ActionStoreData.storeDataInExcelColumn(LocatorOfData,
	 * colName));
	 * 
	 * 
	 * }
	 */

}
